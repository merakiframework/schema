# Static analysis of the shipped code. Tests are analysed separately, at a lower level,
# by phpstan-tests.neon — see the note there.
#
# Level 5 is what src passes, with three narrow ignore entries and no baseline. The cost of
# going higher, measured rather than guessed:
#
#   level 6 — 97 errors
#   level 7 — 136
#   level 8 — 138
#
# Level 6 is a different kind of work — missing generic and iterable types in docblocks, not
# unsound code — and it is tracked in docs/ROADMAP.md rather than being done here.
#
# ## What used to be here, and how it went
#
# Levels 4 and 5 cost 101 errors until 2.0, from one cause: a field is a `readonly` class, so
# PHPStan types every property as *the value the constructor assigned it*, and it cannot see
# `Definition::with()`, which changes them by cloning with a string-keyed array. A
# `public ?int $maxLength` initialised to `null` was inferred as `null` and nothing else, so
# the `!== null` guard in front of every use of it narrowed to *NEVER*.
#
# The two entries that covered that are gone, and they were the ones worth being rid of: they
# matched by pattern across `src/Field/*.php`, so a *genuine* inverted guard written the same
# way would have been suppressed with them. What replaced them is
# `Field\Definition::initially()` — a `mixed`-returning seam every configuration property is
# initialised through, which keeps the property at its declared type. The docblock there
# explains the trade; the short version is that a property nothing ever configures no longer
# has its dead guard reported, and the wither is what makes a property configuration.
#
# Deleting the two entries also turned up four genuinely dead members that the noise had been
# hiding: `Date::mustParse()` and three private statics on `PhoneNumber`, left over from the
# composite-to-value-object rewrite.
#
# ## No baseline file, by design
#
# Errors get fixed, not recorded. The three entries below are limitations of the analyser
# rather than faults in the code, they name one class and one message each, and PHP 8.6's
# readonly property defaults remove all three.
parameters:
    level: 5
    paths:
        - src
    treatPhpDocTypesAsCertain: false

    ignoreErrors:
        # $constraints is declared by Field\Definition and assigned by the *using* class's
        # constructor, as its last statement — the trait cannot assign it, because the properties
        # it is built from are set by that constructor. PHPStan models a readonly property as
        # assignable only in the declaring class's own constructor, so it reports both the
        # declaration and any method reachable from it as unsafe.
        #
        # The code is correct, and the two things that could make it incorrect — a constructor
        # that never assigns it, and a wither that fails to rebuild it — are checked at runtime
        # for every field by Api\SealedFieldTest. That is a stronger guarantee than the analyser
        # was offering, not a weaker one.
        -
            message: '#has an uninitialized readonly property \$constraints\.#'
            path: src/AtomicField.php
        # Same cause, one step further out: defineConstraints() runs during construction and
        # *creates* first-class callables, so PHPStan analyses their bodies in a constructor
        # context. The callables are not invoked until validation, long after
        # initialiseDefinition() has set $optional.
        -
            message: '#Access to an uninitialized readonly property .+::\$optional\.#'
            path: src/Field/Collection.php
        # Field\Definition is a *trait*, and initialiseDefinition() assigns $optional and
        # $defaultValue from it. PHPStan models a readonly property as assignable only in the
        # declaring class's own constructor, and a trait cannot satisfy that however the using
        # class calls it.
        #
        # The alternative is duplicating two assignments into all nineteen field constructors to
        # satisfy the analyser, which trades a real design — one shared half, one varying half —
        # for an inference detail.
        #
        # PHP 8.6's readonly property defaults remove this entirely: `public bool $optional = false;`
        # on the declaration replaces the initialiser and the call goes with it. Tracked in
        # docs/ROADMAP.md. Until then, Api\SealedFieldTest checks at runtime that every field's
        # inherited state really is initialised, which is the guarantee this entry is standing in
        # for.
        -
            message: '#Readonly property .+::\$(optional|defaultValue) is assigned outside of the constructor\.#'
            path: src/Field/Definition.php
