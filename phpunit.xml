<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/11.3/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         cacheDirectory=".phpunit.cache"
         executionOrder="depends,defects"
         shortenArraysForExportThreshold="10"
         requireCoverageMetadata="true"
         beStrictAboutCoverageMetadata="false"
         beStrictAboutOutputDuringTests="true"
         displayDetailsOnTestsThatTriggerDeprecations="true"
         failOnPhpunitDeprecation="true"
         failOnRisky="true"
         failOnWarning="true">
    <!--
        beStrictAboutCoverageMetadata is off deliberately. With it on, any code a test
        executes but does not name in #[CoversClass]/#[UsesClass] makes the test risky,
        and failOnRisky turns that into a build failure. No test file declares
        #[UsesClass] today, and a single Address test loads 16 core classes, so turning
        it on means roughly 300 new attributes to write and then maintain.

        That is deferred rather than dismissed: the test suite churns substantially in
        1.14.0-beta.2 when per-request state moves to ResolvedField, so the attributes
        would be written twice. Revisit once the seam has landed.

        requireCoverageMetadata stays on, so every test still declares what it covers.
    -->
    <testsuites>
        <testsuite name="default">
            <directory>tests</directory>
        </testsuite>
    </testsuites>

    <source ignoreIndirectDeprecations="true" restrictNotices="true" restrictWarnings="true">
        <include>
            <directory>src</directory>
        </include>
    </source>
</phpunit>
